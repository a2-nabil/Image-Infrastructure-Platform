apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: image-api
  labels:
    app: image-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: image-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
